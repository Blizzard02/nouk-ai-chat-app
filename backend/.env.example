# Server
PORT=3000

# AI provider API keys — leave any of these blank to disable that provider.
# The app detects missing keys at runtime and surfaces a clear error in the
# UI instead of crashing.
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
GEMINI_API_KEY=
